server.port=${PORT:3000}

# Serve existing frontend files from project root
spring.web.resources.static-locations=classpath:/static/,file:${user.dir}/

# Load local env file (KEY=VALUE) if present
spring.config.import=optional:file:./trangherb.env
